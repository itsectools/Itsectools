import type { NextConfig } from "next";

const nextConfig: any = {
  /* config options here */
  // @ts-ignore
  devIndicators: {
    buildActivity: false,
    appIsrStatus: false,
  },
};

export default nextConfig;
